services:
  coffee-leaf-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - DEVICE=${DEVICE:-cpu}
    working_dir: /app/run
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
